//#region lib/types/index.js
/**
 * Approval-card shell syntax highlighting, host half.
 *
 * This package contributes browser presentation only. The empty `apply` gives
 * the Cordis Loader a host-side row, which is what lets
 * `@deepseek-ai/dsh-client-modules` find this package's `dsh.client`
 * declaration and serve the browser half through `exports["./client"]`.
 */
/** Host plugin body — no host-side behavior for this surface plugin. */
export function apply() {}
//#endregion
